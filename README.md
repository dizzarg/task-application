# task-application
The simple todo application where I used  JDBC, Swing and Servlet API, maven build tools and own Json parser implementation. 

#### The project has JSON util library. The library can conver java object to JSON and vice versa. ####
